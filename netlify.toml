# Netlify build configuration
[build]
  publish = "dist/sms-client/browser"
  command = "npm run build:prod"

# Redirect all routes to index.html for Angular routing
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
